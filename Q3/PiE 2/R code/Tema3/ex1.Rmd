---
title: "ex4"
author: "Haokang Chen"
date: "2024-09-30"
output: pdf_document
---
# Ex 1
## Apartat a)

```{r}
x = c(58, 50, 60, 65, 64, 62, 56, 57)
upper = mean(x) + qnorm(0.975)*(3/sqrt(8))
lower = mean(x) - qnorm(0.975)*(3/sqrt(8))

#dnorm(x) #densidad
#pnorm(x) #probabilitat acumulada per sota
#qnorm(p) #quantil on es deixa una probabilitat p per sota de la funció de probabilitat acumulada
```

## Apartat b) 

```{r}
x = c(58, 50, 60, 65, 64, 62, 56, 57)
upper = mean(x) + qt(0.95, length(x)-1)*sqrt(var(x))/sqrt(length(x))
lower = mean(x) - qt(0.95, length(x)-1)*sqrt(var(x))/sqrt(length(x))
```

## Apartat c)
```{r}
x = c(58, 50, 60, 65, 64, 62, 56, 57)
upper = ((length(x)-1)*var(x))/qchisq(0.025, length(x)-1)
lower = ((length(x)-1)*var(x))/qchisq(0.975, length(x)-1)

```

rm(lower, upper, x)

# Ex2
```{r}
upper = sqrt((9*12.4*10/9)/qchisq(0.05, 9))
lower = sqrt((9*12.4*10/9)/qchisq(0.95, 9))
```
